<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">BestArticles</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item <%= (()=> {
                    if(page==="Register") {return " active" }})() %>">
                    <a class="nav-link" href="/register">Signup</a>
                </li>
                <li class="nav-item <%= (()=> {
                    if(page==="Login" ) {return " active" }})() %>">
                    <a class="nav-link" href="/login">Signin</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (()=> {
            if(page===" Register" || "Login" ) {return " d-none" } else {return " d-block" } })() %>"
                        href="#">AllArticles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (()=> {
            if(page===" Register" || "Login" ) {return " d-none" } else {return " d-block" } })() %>"
                        href="#">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (()=> {
            if(page===" Register" || "Login" ) {return " d-none" } else {return " d-block" } })() %>"
                        href="#">Logout</a>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0<%= (()=> {
                if(page===" Register" || "Login" ) {return " d-none" } else {return " d-block" } })() %>">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <img src="/images/bg.jpg" class="rounded-circle" width="30" height="30" alt="...">
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="#">Profile</a>
                            <a class="dropdown-item" href="#">My Articles</a>
                            <div class="dropdown-item">New Article</div>
                            <hr>
                            <a class="dropdown-item" href="#">Edit Profile</a>
                        </div>
                    </li>
                </ul>
            </form>
        </div>
    </nav>
</header>